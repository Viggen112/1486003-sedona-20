.title-form__list {
  text-align: center;
}
.title-form__main,
.form-name__title,
.contacts-form__title,
.impression-box__checks,
.attractions-box__checks,
.emotion__title {
  font-weight: bold;
  font-size: 18px;
  line-height: 26px;
}
.title-form__text {
  font-weight: normal;
  font-size: 14px;
  line-height: 24px;
}
.form-name__wrapper input,
.contacts input{
  border: 0;
}
.impression-box,
.attractions-box {
  border: 0;
  margin: 0;
  padding: 0;
}
.impression-box label,
.attractions-box label {
  position: relative;
  padding-left: 75px;
}
.form-name__item,
.contacts-form__item,
.impression-box__item {
  font-weight: normal;
  font-size: 14px;
  line-height: 26px;
}
.impression-box label,
.attractions-box label{
  font-weight: bold;
  font-size: 18px;
  line-height: 26px;
  text-transform: none;
}
.emotion__text textarea {
  border: none;
  width: 279px;
  height: 160px;
}
placeholder {
  font-weight: bold;
  font-size: 18px;
  line-height: 26px;
  color: @black;
}
